name: Docker Image CI

on: [push]

jobs:

  build:
 
    runs-on: ubuntu-latest
 
    steps:
    - uses: actions/checkout@v1
    - name: Build the Docker image
      run: pwd && ls -lh && docker build java/java12 --file Dockerfile --tag my-image-name:$(date +%s)
